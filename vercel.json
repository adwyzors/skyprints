{
    "version": 2,
    "builds": [
        {
            "src": "apps/backend/dist/main.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "apps/backend/dist/**",
                    "apps/packages/contracts/dist/**",
                    "node_modules/.prisma/**"
                ]
            }
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/apps/backend/dist/main.js"
        }
    ],
    "installCommand": "npm install",
    "buildCommand": "npm run build:contracts && npm --workspace apps/backend run build"
}